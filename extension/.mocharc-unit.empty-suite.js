// FSP-003 Pillar C — empty unit suite placeholder.
//
// Mocha errors when `spec` resolves to zero files; this placeholder
// gives it one trivially-passing test until real unit tests get
// migrated into test/unit/.

suite('FSP-003 T0 — empty unit tier', () => {
  test('placeholder until tests migrate to test/unit/', () => {
    // Intentionally empty — see .mocharc-unit.cjs.
  });
});
